{
  "mcpServers": {},
  "settings": {
    "alwaysApproveEnterPlanMode": false,
    "alwaysApproveTools": [],
    "approveAllToolsAfterDelay": false
  },
  "contextFiles": [
    ".claude/context/shopify-theme.md",
    ".claude/context/tech-stack.md",
    "../claude.md",
    "config/settings_schema.json"
  ],
  "rules": [
    "Use Liquid template syntax for all Shopify theme files",
    "Follow Shopify theme best practices and section/block architecture",
    "Ensure all components are responsive and accessible",
    "Use CSS variables defined in snippets/css-variables.liquid",
    "When implementing Figma designs, match the design system exactly",
    "Always test theme changes locally before suggesting deployment",
    "Follow the existing code style and structure in the theme",
    "Use Shopify's schema format for section settings",
    "Optimize images and assets for web performance",
    "NEVER write inline JavaScript in Liquid files. Instead: (1) Create a separate .js file in the assets folder following the same naming convention as CSS files (e.g., section-header.css → section-header.js), (2) Write all JavaScript code in this external file, (3) Link the JavaScript file in the Liquid file using {{ 'filename.js' | asset_url | script_tag }}. This applies to all <script> blocks except for: Alpine.js inline directives (x-data, x-init), critical inline scripts required for performance, or vendor scripts loaded via CDN."
  ]
}
